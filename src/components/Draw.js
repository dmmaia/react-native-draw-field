const {default: DrawArea} = require('../DrawArea');

import React from 'react';
import Svg, {Path} from 'react-native-svg';

function Draw(props) {
  return (
    <Svg>
      <Path />
    </Svg>
  );
}

export default Draw;
