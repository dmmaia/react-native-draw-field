const {default: DrawArea} = require('../DrawArea');

import React from 'react';
import Svg, {Path} from 'react-native-svg';

function Draw() {
  return (
    <Svg>
      <Path />
    </Svg>
  );
}

export default Draw;
